import AdminSubscriptionsClient from "./subscriptions-client";

export const metadata = { title: "구독 관리 — FlowPack Admin" };

export default function AdminSubscriptionsPage() {
  return <AdminSubscriptionsClient />;
}
