import AdminUserDetailClient from "./user-detail-client";

export const metadata = { title: "유저 상세 — FlowPack Admin" };

export default function AdminUserDetailPage() {
  return <AdminUserDetailClient />;
}
